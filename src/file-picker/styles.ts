import { alpha, Button as MaterialButton, IconButton } from "@mui/material";
import { styled } from "@mui/material/styles";

const PREFIX = "FilePicker";

export const filePicker = {
  root: `${PREFIX}-root`,
  text: `${PREFIX}-text`,
};
