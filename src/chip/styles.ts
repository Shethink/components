

const PREFIX = "Modal";

export const hipClasses = {
  root: `${PREFIX}-root`,
  center: `${PREFIX}-center`,
  restricted: `${PREFIX}-restricted`,
  padded: `${PREFIX}-padded`,
  dark: `${PREFIX}-dark`,
};
